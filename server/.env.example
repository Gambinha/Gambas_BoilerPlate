# Copie este arquivo para .env e ajuste os valores conforme necessario
# NUNCA commite o arquivo .env no repositorio

DATABASE_URL=postgresql://postgres:postgres@db:5432/socios_hub

NODE_ENV=development
PORT=3000

# JWT (preencha apos integrar o boilerplate)
JWT_SECRET=troque-por-um-segredo-forte
JWT_EXPIRES_IN=7d
